fastapi>=0.115
uvicorn[standard]>=0.30
asyncpg>=0.29
pydantic>=2.7
pydantic-settings>=2.3

# Test framework (T001) — justification: no backend test framework existed
# prior to the SSO configuration feature, and the plan requires focused
# backend/route-level validation.
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27

# OIDC client (Foundational) — justification: the SSO feature requires a
# real OIDC authorization-code handshake and ID-token/JWKS verification;
# Authlib is the standard, actively maintained library for this in the
# Starlette/FastAPI ecosystem rather than hand-rolling token verification.
authlib>=1.3

# Session signing (T033) — justification: hard transitive requirement of
# Starlette's SessionMiddleware, itself required by Authlib's Starlette OAuth
# client for state/nonce storage during the redirect round trip; reused for
# the app's own post-login session cookie (see research.md).
itsdangerous>=2.1
